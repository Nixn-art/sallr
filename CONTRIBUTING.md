# Contributing

Before review, run `npm test`, `npm run lint`, `npm run check`, and `npm audit --omit=dev`. Changes must preserve authorization boundaries, validate untrusted input, avoid sensitive logging, document API/privacy changes, and include regression tests for fixes. Reviewers check correctness, security, privacy, error handling, performance, accessibility, and compatibility.
